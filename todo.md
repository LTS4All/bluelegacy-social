# Project TODO

- [x] Add BlueLegacy account registration with only username, password, and age fields
- [x] Validate age eligibility on the server and persist age-confirmation status
- [x] Hash BlueLegacy passwords securely and never store plaintext passwords
- [x] Issue and validate authenticated BlueLegacy sessions
- [x] Add the BlueLegacy account website and clear Completed confirmation page
- [x] Add shared BlueLegacy users and posts database tables
- [x] Add iOS-friendly registration, login, session, feed, and create-post API procedures
- [x] Add shared BlueLegacy feed UI with authenticated composer
- [x] Add BlueLegacy Account menu entry to Bluesky Legacy
- [x] Add Login with BlueLegacy hosted account flow and return-to-app behavior
- [x] Add switchable BlueLegacy mode while preserving Bluesky mode
- [x] Prepare GitHub Pages static-site deployment configuration and documentation
- [x] Integrate BlueLegacy mode and API calls into the armv7 Bluesky Legacy client
- [x] Add Vitest coverage for age validation, password handling, sessions, feed access, and post creation
- [x] Run type checks, tests, and browser validation
- [x] Save the final project checkpoint and deliver website/backend/iOS source artifacts
- [x] Defer replacing the temporary Manus HTTPS API URL; GitHub Pages-only testing is confirmed
- [x] Use GitHub Pages account-site deployment with the temporary HTTPS backend origin for testing
- [x] Fix the GitHub Pages workflow’s pnpm version conflict and rerun the public Pages deployment
- [x] Publish the account site at https://lts4all.github.io/bluelegacy-social/ with HTTPS enforced
- [x] Configure the iOS client to open GitHub Pages for account flows while using the HTTPS backend for API calls
- [x] Fix blank GitHub Pages site by setting the Vite base path to `/bluelegacy-social/`, redeploying, and verifying loaded assets
- [x] Verify the versioned GitHub Pages URL renders the BlueLegacy Home screen; normal URL cache behavior is documented
- [x] Add deterministic cache-busting or versioning so stale GitHub Pages bundles cannot keep showing the old 404 state
- [x] Make the GitHub Pages account flow compatible with iOS 9 Safari and older WebKit JavaScript/CSS behavior
- [x] Redesign the BlueLegacy account UI with friendly atomic components, high contrast, clear labels, and compact mobile layout
- [x] Redeploy the legacy-compatible Pages build and rebuild the armv7 IPA with the updated versioned web URL
- [x] Add an optional QR code panel to the BlueLegacy account UI linking to the GitHub Pages account flow, with a typed-URL fallback
- [x] Preserve `returnTo=bluelegacy` in the legacy page and deep-link back with the issued session token after login or registration
- [x] Add a legacy-compatible Completed and Return to Bluesky Legacy state
- [x] Verify the legacy account handoff contract against the rebuilt armv7 IPA; physical iPod installation remains user-side validation
- [ ] Automatically launch `bluelegacy://auth?token=...` after successful legacy login or registration when `returnTo=bluelegacy`, while retaining the visible fallback button
